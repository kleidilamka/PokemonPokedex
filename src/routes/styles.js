import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
  },
  text: {
    fontSize: 18,
    color: '#fff',
    paddingTop: 2,
    paddingRight: 2,
  },
});

export default styles;
